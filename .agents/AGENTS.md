# Project-Specific Rules

- **Auto-Sync to GitHub**: Every time we modify code files, fix bugs, or update documentation, automatically stage (`git add .`), commit with a descriptive message (`git commit -m "..."`), and push the updates (`git push origin main`) at the end of the turn.
